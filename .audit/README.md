# Auditoría independiente de GI-OT

`.audit/` evalúa el mecanismo y el estado del repositorio de forma
independiente del Reviewer, CI y HITL. No contiene reglas de producto, no
decide merges y no copia evidencias del Template.

Toda evidencia debe identificar commit, fecha, comando y resultado real.
